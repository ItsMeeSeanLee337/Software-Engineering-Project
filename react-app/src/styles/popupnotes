.flexNotes{
    flex:center;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    margin: 10px;
    text-align: center;
}

.showNotesPopUp {
    position: absolute;
    top: 50%; /* Vertically center the box */
    left: 50%; /* Horizontally center the box */
    transform: translate(-50%, -50%); /* Center the box using a transform */
    width: 70%; /* Adjust this to the desired width for the pop-up content */
    max-height: 100%; /* Adjust this to the desired maximum height for the pop-up content */
    overflow-y: auto; /* Add vertical scroll when content exceeds the maximum height */
    background-color: #fff; /* Background color for the pop-up */
    padding: 20px;
    border: 1px solid #ccc;
    z-index: 100; /* Set a high z-index to ensure it's on top of everything else */
  }
  

  .showNotesPopUpContext {
    position: relative;
    background-color: #fff; /* Background color for the pop-up */
    padding: 20px;
    border: 1px solid #ccc;
    z-index: 101; /* Higher z-index to ensure it's above the overlay */
    margin:10px;
  }

.notesTextArea{
    border: 1px solid #752f2f;
}

textarea{
    border: none; /* Remove the border */
  padding: 10px; /* Add padding for spacing within the textarea */
  background-color: #f0f0f0; /* Background color (you can change this) */
  font-size: 14px; /* Font size (you can change this) */
  resize: none; /* Disable resizing of the textarea */
  outline: none; /* Remove the default focus outline (you may want to add a custom focus style) */
  /* Add other styles as needed */
}

